#pragma once

#include <string>
#include <vector>

#include "lexer.h"

class State {
private:
	Lexstate state;


public:
	State(void) = default;
	State(const State &) = delete;
	virtual ~State(void) = default;






};
